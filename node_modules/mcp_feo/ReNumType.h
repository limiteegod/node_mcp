#ifndef RENUMTYPE_H
#define RENUMTYPE_H

#include <stdlib.h>
#include <stdio.h>


/**
 * 任1到任4的号码
 */
class ReNumType {
    public:
        ReNumType(char *pNum, int len);
        ~ReNumType();

    private:
        long *pos;  //指针数组，存放任选号码的4位，序号从0开始
        int *posLen;    //存放号码4个位的长度
};

#endif
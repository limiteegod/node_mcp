#ifndef INTARRAY_H
#define INTARRAY_H

class IntArray {
    public:
        IntArray(int volume);
        IntArray(int volume, char *p, int len, char sep);
        ~IntArray();

        void put(int value);
        int get(int index);
        int length();

    private:
        int* pInt;  //数据存储区
        int curLen; //当前长度
        int volume; //当前最大容量
};

#endif